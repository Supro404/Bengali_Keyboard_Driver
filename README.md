# Bengali_Keyboard_Driver
A simple project to implement keyboard driver. Languages can be modified from bengali.
**NOTE**: Windows only

To run
```
pip install keyboard
python driver.py
```

To exit press `ESC`
